{
  "name": "pagearm",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "PageArm. A personal browser runtime for Chromium, Firefox, and Safari on Mac, Windows, and Linux: install a thin shell once, keep the agent on a URL.",
  "license": "BUSL-1.1",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "pack": "node scripts/pack.mjs",
    "check": "node scripts/check.mjs"
  }
}
